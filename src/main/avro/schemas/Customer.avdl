@namespace("com.jana.karim.avro.model.destination")
protocol Customer {
    record Name {
      string id;
      string name;
      string nickname;
    }
    record MailAddress {
      array<string> city;
    }
}